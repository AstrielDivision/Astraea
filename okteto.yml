name: north
image: node:alpine
sync: false
command: apk update && apk add libtool autoconf automake g++ gcc git make && apk add --no-cache bash && git init && yarn && yarn build && yarn global add pm2 && pm2 start . && bash
forward:
- 9229:9229
- 3000:3000
